version: '3.8'
services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - DATA_DIR=/root/data
      - JWT_SECRET=your_secret_key_change_me
    volumes:
      - ./data:/root/data
      - ./log.txt:/root/log.txt